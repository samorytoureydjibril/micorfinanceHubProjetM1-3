# Server Configuration
server.port=8084

# Eureka Configuration
eureka.client.service-url.defaultZone=http://localhost:8761/eureka/
eureka.client.fetch-registry=true
eureka.client.register-with-eureka=true
eureka.instance.prefer-ip-address=true

# Database Configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/microfinance_repayment
spring.datasource.username=postgres
spring.datasource.password=okomothom-96
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true

# Flyway Configuration
spring.flyway.enabled=true
spring.flyway.baseline-on-migrate=true

# RabbitMQ Configuration
spring.rabbitmq.host=localhost
spring.rabbitmq.port=5672
spring.rabbitmq.username=guest
spring.rabbitmq.password=guest

# Client Service URL
client.service.url=http://localhost:8081

# Loan Service URL
loan.service.url=http://localhost:8083

# JWT Configuration
jwt.secret=404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970

# Penalty Configuration
repayment.penalty.rate=0.05
repayment.penalty.grace-period-days=3
repayment.penalty.enabled=true

# Logging
logging.level.com.microfinancehub.micorhub_repayment_service=DEBUG
logging.level.org.springframework.security=DEBUG

# Management Endpoints
management.endpoints.web.exposure.include=health,info,metrics

internal.api.token=my-super-secret-internal-token-2026